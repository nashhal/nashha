name: News Research

on:
  workflow_dispatch:

permissions:
  contents: write

# هذا workflow لم يعد يجمع الأخبار من أي قناة أو موقع إخباري.
# الجمع الوحيد للأخبار يتم عبر news_bot_v2.py من المصادر الرسمية/الأولية.
jobs:
  guard:
    runs-on: ubuntu-latest
    steps:
      - name: Verify source policy
        run: echo 'News Research disabled: official-primary-source workflow only.'
